version: '3.8'
services:
  tradingview-mcp:
    build: .
    image: 'atilaahmet/tradingview-mcp:latest'
    container_name: tradingview-mcp
    # Hapus bagian ports: - '8081:8000'
    restart: unless-stopped
    environment:
      - PORT=8000
      - HOST=0.0.0.0
